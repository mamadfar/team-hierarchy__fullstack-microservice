/** Injection token for the shared pg Pool. */
export const PG_POOL = Symbol('ORBIT_PG_POOL');

/** Injection token for the drizzle instance bound to @orbit/shared/db tables. */
export const DRIZZLE = Symbol('ORBIT_DRIZZLE');
